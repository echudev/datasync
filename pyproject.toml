[project]
name = "datasync"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.2.3",
    "pyserial>=3.5",
    "ruff>=0.9.7",
    "tenacity>=9.0.0",
]
